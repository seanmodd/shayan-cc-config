#!/bin/bash
# Double-click to install the `shayan` command (one-word Claude Code setup switching).
curl -fsSL https://shayan-cc-config.vercel.app/shayan.sh | bash
echo "Press any key to close…"; read -n 1
